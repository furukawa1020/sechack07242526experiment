# テスト報告

対象プロトコル: `R8-010-2x2-mock-v1`

最終確認日: 2026-07-19（Node.js 24 / Windows）

| コマンド | 結果 | 実行日時 |
|---|---|---|
| `npm run lint` | 成功 | 2026-07-19 |
| `npm run typecheck` | 成功 | 2026-07-19 |
| `npm test` | 成功: 14ファイル、125テスト | 2026-07-19 |
| `npm run test:e2e` | 成功: 7テスト | 2026-07-19 |
| `npm run build` | 成功 | 2026-07-19 |
| `npm run preflight -- --allow-mock` | 成功（開発用Mock確認） | 2026-07-19 |
| `npm run preflight` | 予定どおり失敗（実機・COM・Google Forms未設定） | 2026-07-19 |

単体・統合テストのカバレッジは、Statements 92.84%、Branches 85.78%、Functions 93.90%、Lines 93.42%だった。安全上重要な`SerialPufferDevice`と`SessionController`も集計対象に含む。E2Eはテスト用MockDeviceとローカルサーバだけを使用し、実行後に待受ポートが解放されることも確認した。本番モードは通常設定のMockDeviceを起動前に拒否した。テスト専用の本番許可設定を明示したビルド成果物では、`GET /healthz`がHTTP 200と正しいプロトコルバージョンを返した後、正常終了できることを確認した。

## 自動確認範囲

- 条件A〜Dの固定対応
- 4提示順、位置均衡、直前→直後ペア均衡
- A/Bの右パネル同一性
- C/Dの右パネルおよび装置命令同一性
- 固定値のセッション内ロック
- 不正な状態遷移の拒否
- MockDevice正常系と障害注入
- STOP優先とDEFLATE
- 直列通信のACK/NACK、タイムアウト、状態矛盾、遅延ACK
- 装置命令の発行・ACK監査ログとログ保存障害時のfail-closed
- ログ許可フィールドとPII拒否
- 参加者画面の内部コード非表示
- localhost以外への自動通信禁止
- 画面切断、スタッフ画面切断、リロード、緊急停止、重複研究用ID
- 1366×768、1920×1080のオーバーフロー
- 通常設定での本番Mock起動拒否と事前確認ゲート

## 画面確認

次の7状態を1366×768と1920×1080の両方で撮影し、計14枚を`artifacts/screenshots/`へ保存した。

- スタッフ画面
- 進行中のスタッフ画面
- デバイステスト画面
- 共通導入
- 状態ラベルの結果提示
- フグの結果提示
- 4提示のサマリー

参加者画面に内部コードA/B/C/D、研究用ID、内部提示順が表示されないこと、画面の主要操作・固定文言が切れないことを目視確認した。

## 人による確認が必要な項目

- USBシリアル実機との疎通
- 実機側の圧力クランプと通信断フェイルセーフ
- 物理緊急停止
- 空気漏れと収縮完了
- 会場での可読性と設置方向
- Googleフォームの同意・11問・QR
- 研究責任者による文言と実施方式の最終承認
